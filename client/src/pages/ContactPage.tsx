import { useState, type FormEvent } from "react";
import { Link, useSearchParams } from "react-router-dom";
import { CorporateHero } from "../components/company/CorporateHero";

export function ContactPage() {
  const [params] = useSearchParams();
  const [prepared, setPrepared] = useState(false);
  function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    const data = new FormData(event.currentTarget);
    const body = `Name: ${data.get("name")}\nEmail: ${data.get("email")}\nOrder number: ${data.get("order") || "Not applicable"}\n\n${data.get("message")}`;
    window.location.href = `mailto:hello@buzimabooster.rw?subject=${encodeURIComponent(String(data.get("topic")))}&body=${encodeURIComponent(body)}`;
    setPrepared(true);
  }
  const field = "mt-2 w-full rounded-xl border border-slate-300 bg-white px-4 py-3 text-sm outline-none focus:border-sky-500 focus:ring-4 focus:ring-sky-100";
  return <main className="bg-slate-50"><CorporateHero eyebrow="Contact us" title="Let’s find your next step." description="Ask about a product, discuss equipment for your business, or get help with an existing order." /><div className="mx-auto grid max-w-6xl gap-10 px-5 py-16 lg:grid-cols-[340px_1fr] lg:px-8"><aside><h2 className="text-2xl font-black">Speak with our team</h2><p className="mt-4 text-sm leading-7 text-slate-600">For order enquiries, include your order number and product details. For business purchases, share your required quantities and specifications.</p><div className="mt-6 rounded-2xl border border-slate-200 bg-white p-6"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Email</p><a className="mt-2 block break-all font-bold text-sky-600 hover:underline" href="mailto:hello@buzimabooster.rw">hello@buzimabooster.rw</a><p className="mt-6 text-xs font-bold uppercase tracking-wider text-slate-500">Location</p><p className="mt-2 font-semibold">Kigali, Rwanda</p></div><h3 className="mt-8 font-bold">Quick answers</h3><div className="mt-3 flex flex-col gap-3 text-sm font-semibold text-sky-700">{[["/delivery", "Delivery information"], ["/returns", "Returns & exchanges"], ["/warranty", "Warranty support"], ["/account/orders", "View my orders"]].map(([to, label]) => <Link className="hover:underline" key={to} to={to}>{label} →</Link>)}</div></aside><section className="rounded-2xl border border-slate-200 bg-white p-6 sm:p-8"><h2 className="text-2xl font-black">Prepare your enquiry</h2><p className="mt-3 text-sm leading-6 text-slate-500">Complete the details below to open a prepared message in your email app. Review it and send it from there.</p><form className="mt-7 space-y-5" onSubmit={submit}><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold">Full name<input autoComplete="name" className={field} maxLength={120} name="name" required /></label><label className="text-sm font-semibold">Email address<input autoComplete="email" className={field} maxLength={254} name="email" required type="email" /></label></div><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold">Enquiry topic<select className={field} defaultValue={params.get("topic") === "Business enquiry" ? "Business enquiry" : "Product enquiry"} name="topic">{["Product enquiry", "Business enquiry", "Delivery enquiry", "Return request", "Warranty enquiry", "Other enquiry"].map((topic) => <option key={topic}>{topic}</option>)}</select></label><label className="text-sm font-semibold">Order number <span className="font-normal text-slate-400">(optional)</span><input className={field} maxLength={80} name="order" /></label></div><label className="block text-sm font-semibold">How can we help?<textarea className={field} maxLength={3000} name="message" required rows={6} /></label><button className="rounded-xl bg-sky-500 px-6 py-3 text-sm font-bold text-white hover:bg-sky-600" type="submit">Open email enquiry →</button>{prepared && <p className="text-sm leading-6 text-slate-600" role="status">Your email draft is prepared. Send it in your email app to contact us. If no app opened, email hello@buzimabooster.rw directly.</p>}</form></section></div></main>;
}
