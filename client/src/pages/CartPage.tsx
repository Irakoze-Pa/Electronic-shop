import { Link } from "react-router-dom";
import { useCart } from "../cart/useCart";
import { ErrorState } from "../components/ui/AsyncState";
import { currencyFormatter } from "../utils/format";

export function CartPage() {
  const cartState = useCart();
  if (cartState.loading && !cartState.cart.items.length) return <main className="mx-auto min-h-[60vh] max-w-7xl px-5 py-12"><div className="h-80 animate-pulse rounded-3xl bg-slate-200" /></main>;
  if (cartState.error && !cartState.cart.items.length) return <main className="mx-auto min-h-[60vh] max-w-7xl px-5 py-12"><ErrorState message={cartState.error} onRetry={() => void cartState.refreshCart()} /></main>;
  if (!cartState.cart.items.length) return <main className="mx-auto grid min-h-[60vh] max-w-2xl place-items-center px-5 py-16 text-center"><div><div className="text-6xl">🛒</div><h1 className="mt-5 text-3xl font-black">Your cart is empty</h1><p className="mt-3 text-slate-500">Explore our products and add something you love.</p><Link className="mt-7 inline-block rounded-xl bg-[#1F88C9] px-6 py-3 font-bold text-white" to="/shop">Continue shopping</Link></div></main>;
  return <main className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-wider text-[#1F88C9]">Shopping cart</p><h1 className="mt-2 text-4xl font-black">Your selections</h1></div><button className="text-sm font-bold text-red-600 disabled:opacity-50" disabled={cartState.loading} onClick={() => void cartState.clearCart()} type="button">Clear cart</button></div>{cartState.error && <p className="mt-6 rounded-xl bg-red-50 p-3 text-sm text-red-700">{cartState.error}</p>}<div className="mt-9 grid gap-8 lg:grid-cols-[1fr_360px]"><section className="space-y-4">{cartState.cart.items.map(({ product, quantity, lineSubtotal }) => <article className="grid grid-cols-[88px_1fr] gap-4 rounded-2xl border border-slate-200 bg-white p-4 sm:grid-cols-[110px_1fr_auto]" key={product._id}><Link className="aspect-square overflow-hidden rounded-xl bg-slate-100" to={`/products/${product._id}`}>{product.images[0] ? <img alt={product.name} className="h-full w-full object-cover" src={product.images[0]} /> : <span className="grid h-full place-items-center font-black text-[#1F88C9]/30">BBG</span>}</Link><div><Link className="font-black hover:text-[#1F88C9]" to={`/products/${product._id}`}>{product.name}</Link><p className="mt-1 text-sm text-slate-500">{currencyFormatter.format(product.price)} each</p><p className={`mt-2 text-xs font-semibold ${product.stock >= quantity ? "text-emerald-600" : "text-red-600"}`}>{product.stock ? `${product.stock} available` : "Out of stock"}</p><div className="mt-4 flex items-center gap-3"><div className="flex items-center rounded-lg border border-slate-300"><button aria-label="Decrease quantity" className="size-9 disabled:text-slate-300" disabled={quantity <= 1 || cartState.busyProductId === product._id} onClick={() => void cartState.updateQuantity(product._id, quantity - 1)} type="button">−</button><span className="w-8 text-center text-sm font-bold">{quantity}</span><button aria-label="Increase quantity" className="size-9 disabled:text-slate-300" disabled={quantity >= product.stock || cartState.busyProductId === product._id} onClick={() => void cartState.updateQuantity(product._id, quantity + 1)} type="button">+</button></div><button className="text-sm font-bold text-red-600" disabled={cartState.busyProductId === product._id} onClick={() => void cartState.removeFromCart(product._id)} type="button">Remove</button></div></div><p className="col-span-2 font-black sm:col-span-1">{currencyFormatter.format(lineSubtotal)}</p></article>)}</section><aside className="h-fit rounded-2xl bg-slate-950 p-6 text-white lg:sticky lg:top-36"><h2 className="text-xl font-black">Cart summary</h2><div className="mt-6 flex justify-between text-sm text-slate-300"><span>Total items</span><span>{cartState.cartCount}</span></div><div className="mt-4 flex justify-between border-t border-white/15 pt-4 text-lg font-black"><span>Subtotal</span><span>{currencyFormatter.format(cartState.subtotal)}</span></div><p className="mt-3 text-xs text-slate-400">Delivery and taxes will be calculated during checkout.</p><button className="mt-6 w-full cursor-not-allowed rounded-xl bg-slate-700 px-5 py-3 font-bold text-slate-300" disabled type="button">Checkout coming soon</button><Link className="mt-4 block text-center text-sm font-bold text-[#69bceb]" to="/shop">Continue shopping</Link></aside></div></main>;
}
